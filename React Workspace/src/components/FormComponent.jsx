// FormComponent.jsx
import React from 'react';

const FormComponent = ({ formData, handleChange, handleImageChange, handleSubmit, handleIncrement, handleDecrement }) => {
  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {/* Form fields */}
    </form>
  );
};

export default FormComponent;
